# SPEC-058: UI デザインプロトタイプ（絵本寄り / ゲーム HUD 寄り）

| 項目 | 内容 |
|---|---|
| 仕様ID | SPEC-058 |
| 機能名 | UI デザインプロトタイプ比較 |
| 対応ファイル | `prototype/index.html` `.design-toggle`, `#daily-focus-card`, `#screen-record` / `prototype/game.js` `setDesignTheme()`, `renderDailyFocusCard()`, `renderRecordScreen()` / `prototype/styles.css` `.theme-picturebook`, `.theme-gamehud`, `.daily-focus-card`, `.record-*` |
| 関連仕様 | SPEC-002, SPEC-009, SPEC-034, SPEC-048, SPEC-051, SPEC-055 |
| ステータス | Active |
| 最終更新 | 2026-04-26 |

## 1. 目的

現行 UI の方向性を比較判断するため、同じゲーム状態を **絵本寄り** と **ゲーム HUD 寄り** の 2 種類で見比べられるプロトタイプを追加する。

同時に、S2「遊びを選ぶ」画面へ毎日表示の **今日の焦点カード** を追加し、プレイヤーが「次に何をするとよいか」を迷いにくくする。

## 2. 用語定義

- **絵本寄り**: 紙・余白・やわらかい色・エピソード文を優先する表現。
- **ゲーム HUD 寄り**: 濃色 HUD・高コントラスト・バッジ・数値の読み取りやすさを優先する表現。
- **今日の焦点カード**: S2 の上部に毎日表示される「きょうのめあて」カード。
- **きろく**: SPEC-051 のプロフィール導線を、幼少期向けに言い換えた名称。

## 3. 入力（Input）

- 現在の `player` 状態。
- `player.activeMissions` / `MISSION_SCENARIOS`。
- 遊びマスタ `PLAYS` と各遊びの `gain` / `categories`。
- UI デザインテーマ: `picturebook` または `gamehud`。

## 4. 出力（Output）

- タイトル画面と S2 にデザインテーマ切替 UI を表示する。
- `.phone-frame` に `theme-picturebook` または `theme-gamehud` クラスを付与する。
- S2 に `#daily-focus-card` を毎日表示する。
- ドック末尾に `🌱 きろく` ボタンを表示し、`#screen-record` へ遷移できる。

## 5. 挙動（Behavior）

### 5.1 デザインモード切替

1. 初期表示は `picturebook`。
2. タイトル画面または S2 の切替ボタンを押すと `setDesignTheme(theme)` を呼ぶ。
3. `setDesignTheme()` は `.phone-frame` のテーマクラスを差し替え、切替ボタンの `active` を更新する。
4. ゲーム進行値は変えない。同じ状態を見た目だけ変えて比較する。

### 5.2 今日の焦点カード

S2 を表示するたびに `renderDailyFocusCard()` を呼ぶ。

優先順位:

1. 進行中ミッション（`state === "ACCEPTED"`）がある場合、そのミッションを最優先で表示する。
2. 進行中ミッションがない場合、現在の素養のうち最も伸びているものを見つけ、その素養を伸ばせる実行可能な遊びを最大 2 件提案する。
3. 実行可能な遊びがない場合、「今日はゆっくり休む」提案にする。

表示内容:

- ラベル: `きょうのめあて`
- 見出し: ミッション名または伸ばしたい素養
- 本文: 今日の選択理由
- 推奨遊び: 最大 2 件のピル

### 5.3 遊びアイコンの意味バッジ

S2 ドックの遊びアイコンに、必要に応じて小さなバッジを表示する。

| バッジ | 条件 |
|---|---|
| `めあて` | 今日の焦点カードで推奨された遊び |
| `好き` | 累計プレイ回数が 3 回以上 |
| `新しい` | 累計プレイ回数が 0 回 |
| `つかれる` | `avail.isLowStamina === true` |

### 5.4 きろく画面

`🌱 きろく` ボタン押下で `#screen-record` を表示する。

初期プロトタイプでは以下の 3 カードを表示する。

1. `できたこと`: 獲得済み称号数と最新称号。
2. `だいすきな遊び`: 累計回数上位 3 件。
3. `思い出`: 連絡帳ハイライトまたは忘れられない日。

## 6. UI への反映

- S0 タイトル: デザインモード切替を表示。
- S2 遊び選択: 今日の焦点カード、デザインモード切替、遊びバッジ、`🌱 きろく` 導線を表示。
- S17 きろく: 記録を感情寄りに振り返る簡易画面。

## 7. 想定される利用シナリオ

### 7.1 絵本寄りで確認

タイトルで「絵本」を選ぶ。S2 は紙面風の明るい色、焦点カードは「きょうのめあて」としてやわらかく表示される。

### 7.2 ゲーム HUD 寄りで確認

タイトルまたは S2 で「HUD」を選ぶ。HUD と焦点カードが濃色・高コントラストになり、バッジの視認性が上がる。

### 7.3 きろくを見る

ドックの `🌱 きろく` を押すと、できたこと・好きな遊び・思い出を確認できる。

## 8. 例外・エッジケース

- 進行中ミッションが複数ある場合は `MISSION_SCENARIOS.priority` が高いものを優先する。
- 推奨遊びがロック中の場合は表示候補から外す。
- `player._playCounts` が空の場合、だいすきな遊びカードは「これから増えていく」と表示する。
- `titles` / `renrakuchoHighlights` / `memorableDays` が空の場合、きろく画面は空状態の文言を表示する。

## 9. Why not

### 9.1 Why not: 片方だけを先に実装する

見た目の方向性を早く比較することが目的なので、絵本寄り / HUD 寄りを同時に切り替えられる形にする。

### 9.2 Why not: 今日の焦点カードをミッション中だけ表示する

ユーザー指示により、今回は毎日表示で作成する。ミッションがない日も「今日は何を大切にするか」を示すことで、S2 の迷いを減らす。

### 9.3 Why not: プロフィール導線を `アルバム` にする

ユーザー指示により、導線名は `きろく` とする。`きろく` は分析的すぎず、幼少期にも後年にも使いやすい中間表現である。

### 9.4 Why not: テーマごとに別 HTML を複製する

別ファイル化すると機能差分が生まれ、比較対象として不正確になる。同じ DOM にテーマクラスを当て、見た目だけを切り替える。

## 10. 未決事項・TODO

- 実機確認後、どちらを本採用するか決める。
- S17 きろく画面は SPEC-051 の本実装時にタブ構成へ拡張する。
- 今日の焦点カードの推薦ロジックは、ミッション DSL の条件表現が増えた段階で再調整する。

## 11. 改訂履歴

- 2026-04-26 初版。絵本寄り / ゲーム HUD 寄りの切替、毎日表示の今日の焦点カード、`きろく` 導線を定義。
